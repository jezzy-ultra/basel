PATH_add .bin/.shims

export RA_LOG=rust_analyzer=warn
export DIRENV_WARN_TIMEOUT=5m

watch_file flake.nix
watch_file flake.lock
watch_file shell.nix
watch_file rust-toolchain.toml

use flake || use nix
